"""mdhpp — unified operational CLI for MDHousingPolicyPipeline.

The `mdhpp` console script (see pyproject [project.scripts]) dispatches to
subcommands. Business logic lives in the workspace member packages; this
top-level package is dispatch only.
"""

__version__ = "0.1.0"
